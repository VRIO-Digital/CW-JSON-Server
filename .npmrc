# Fail `npm audit` on ANY advisory, including low severity.
audit-level=low

# Always run the audit that npm prints after an install.
audit=true

# Keep install output focused on real signal.
fund=false
